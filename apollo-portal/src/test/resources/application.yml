server:
  port: 8070
  
spring:
  application:
    name: apollo-portal
  main:
    allow-bean-definition-overriding: true

logging:
  file: /var/log/apollo/apollo-portal.log
  
apollo:
  portal:
    envs: local
