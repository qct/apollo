spring:
  application:
    name: apollo-configservice
  main:
    allow-bean-definition-overriding: true

server:
  port: ${port:8080}
  
logging:
  level:
    org.springframework.cloud: 'DEBUG'
  file: /var/log/apollo/apollo-configservice.log
